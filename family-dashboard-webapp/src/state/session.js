// Auth-derived flags that affect what the signed-in user can see, separate
// from the synced Firestore data in store.js.
export const session = {
  canSeeFinance: false,
  canSeeExpenses: false,
};
